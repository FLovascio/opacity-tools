#pragma once

#include <vector>
#include <functional>

namespace interpolators{
template<class T>
class interp1D{
   private:
   std::vector<double> weights;
   public:
   interp1D(std::vector<T> dataX, std::vector<T> dataY){
      
   }   
};
};